# M0 方法层融合检查表

## 目标

在进入 `#2 主线：认证、租户与 RBAC 基础` 之前，确认方法层已经形成完整闭环，避免后续业务开发重新返工流程与规范。

## 检查项

### 一、仓库内模板是否齐备

- [x] `docs/templates/` 已建立
- [x] workitem 七件套模板已建立
- [x] `docs/workitems/` 已建立并有示例工作包

### 二、双层 skill 是否齐备

- [x] 通用 skill `learning-rd-workflow` 已建立
- [x] 项目 skill `zld-supportpilot-delivery` 已建立
- [x] 两个 skill 都已通过 `quick_validate.py` 校验

### 三、GitHub 流程是否统一中文

- [x] issue 模板已改为中文
- [x] PR 模板已改为中文
- [x] milestones 已改成中文阶段语义
- [x] `#2` ~ `#14` 已统一主线 / 并行 / 融合语义
- [x] 当前 PR #10 已改成中文说明

### 四、任务编排是否可执行

- [x] 已输出当前 issue 依赖与并发分析
- [x] 已输出执行波次规划
- [x] 已明确 `#2` 是后续业务功能的串行前置

### 五、M0 工作包是否完整

- [x] `#11` 工作包完整
- [x] `#12` 工作包完整
- [x] `#13` 工作包完整
- [x] `#14` 工作包完整

### 六、基础验证是否完成

- [x] `go test ./...` 已通过
- [x] 关键文档路径与 GitHub issue / PR 一致

## 结论

当前 M0 已满足“进入业务实现前的最小方法层闭环”。后续可以正式启动 `#2 主线：认证、租户与 RBAC 基础` 的调研、设计与实现准备。
