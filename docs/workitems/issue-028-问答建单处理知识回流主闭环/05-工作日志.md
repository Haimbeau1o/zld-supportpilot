# 工作日志

## 2026-03-09

### 1. 依赖判断

- 确认 `#28` 是融合 issue，不适合与其前置能力并行开发
- 确认前置依赖为 `#25`、`#26`、`#27`
- 检查发现 PR `#35`（即 `#27`）仍为 OPEN，因此 `#28` 暂时不能直接从 `main` 起步做实现
- 结论：采用 stacked worktree，在 `codex/ticket-knowledge-candidates` 基础上继续推进

### 2. 隔离环境

- 检查 `.worktrees/` 已存在且被 `.gitignore` 忽略
- 创建 stacked worktree：`codex/issue-028-main-loop`
- 路径：`/Volumes/passport/简历/go_learn/zld-supportpilot/.worktrees/codex/issue-028-main-loop`

### 3. 基线验证

- 运行关键包基线回归：
  - `./internal/app`
  - `./internal/platform/http`
  - `./internal/module/ai`
  - `./internal/module/knowledge`
- 结果：通过
- 说明：当前基础能力可作为主闭环融合的实现底座

### 4. 方案收敛

- 确认 `#28` 不新增大模块
- 采用“端到端测试先行 + 最小胶水修复 + demo 脚本 + 文档收口”的方案
- 产出设计与实施文档：
  - `docs/plans/2026-03-09-main-loop-fusion-design.md`
  - `docs/plans/2026-03-09-main-loop-fusion-implementation-plan.md`

### 5. RED：主闭环测试先行

- 新增主闭环集成测试：`internal/app/app_main_loop_test.go`
- 测试覆盖链路：
  - 终端用户问答
  - unresolved 反馈
  - 系统建单
  - AI 工单辅助
  - 工单状态流转
  - 知识候选创建与审核
  - 下一轮问答命中新知识
- 首次失败点：`end_user` 调用 `/api/v1/ai/intakes` 被 `403 forbidden` 拦截

### 6. 调研与根因定位

- 回查 AI intake 的真实依赖链路，确认问题不在 HTTP 路由，而在知识 chunk 读取授权
- 发现测试桩与真实应用路径存在差异：
  - 之前某些测试通过 stub `ChunkSource` 运行，因此不会触发知识权限判定
  - 真正的应用集成路径使用 `knowledge.Service` 作为 chunk 来源
- 进一步确认：`ListIndexedChunks()` 沿用了后台知识阅读权限，导致终端用户自助问答被误伤

### 7. GREEN：最小实现修复

- 修改文件：`internal/module/knowledge/service.go`
- 新增 `canReadPublishedKnowledge()` 判定
- 修复原则：
  - 只放开“已发布 chunk”的自助问答读取路径
  - 不放开原始文档与知识管理接口
  - 保持权限边界最小化
- 同时新增知识模块回归测试：`internal/module/knowledge/self_service_access_test.go`

### 8. 人工演示链路补齐

- 新增脚本：`scripts/demo_issue_028_closed_loop.sh`
- README 增加 `#28 主闭环演示` 章节，给出演示方式和限制说明
- 说明当前公开注册流程只创建租户管理员，因此 demo 脚本使用单账号串联流程，多角色分工由自动化测试承担

### 9. Demo 脚本二次调试

- 首次执行脚本失败，报错表现为 JSON body 被拆词
- 通过最小排查确认问题位于 `request_json()` 对 curl 数组命令的执行方式
- 修复后复验通过，说明 README 中的演示入口具备真实可运行性

### 10. 验证收口

- 关键测试通过：
  - `TestListIndexedChunksAllowsEndUserSelfServiceAccess`
  - `TestNewMainLoopFusionSupportsEndUserFeedbackToKnowledgeCycle`
- 包级回归通过：
  - `./internal/app`
  - `./internal/platform/http`
  - `./internal/module/ai`
  - `./internal/module/knowledge`
- 全量回归通过：`./...`
