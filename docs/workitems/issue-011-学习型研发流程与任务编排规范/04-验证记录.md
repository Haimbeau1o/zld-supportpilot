# 验证记录

## 自动化验证

| 命令 | 结果 | 说明 |
| --- | --- | --- |
| `go test ./...` | 待更新 | 本阶段主要改文档，但仍需确认仓库骨架不受影响 |
| `python3 .../quick_validate.py skills/learning-rd-workflow` | 待更新 | skill 结构校验 |
| `python3 .../quick_validate.py skills/zld-supportpilot-delivery` | 待更新 | skill 结构校验 |

## 手工验证

- 检查 README、CONTRIBUTING、路线图是否表达一致
- 检查 workitem 模板是否覆盖 7 件套要求
- 检查依赖分析是否与当前 issue 列表一致

## 边界情况验证

- issue 可以扩展，但不应删除核心字段
- 如果后续 roadmap 变化，应同步更新依赖分析与 skill 引用

## 失败与修正

- 待更新
