# 工作日志

## 2026-03-09

### 1. 分支与隔离环境

- 确认 `#26` 依赖 `#25`
- 基于 `codex/unified-ai-intake` 创建 stacked worktree：`codex/answer-feedback-loop`
- 在新工作树执行基线回归，确认可继续开发

### 2. 代码侦察

- 调研统一受理 service、HTTP handler、ticket service、persistence schema
- 识别出 `#25` 缺少 intake 主记录与 `intake_id`
- 确认现有 `CreateTicket` 能承担反馈升级动作

### 3. 方案收敛

- 确定新增 intake 持久化模型
- 确定新增 feedback 提交与统计接口
- 确定 `unresolved` 走直接建单规则，且必须防止重复建单

### 4. TDD 实现

- 先补 `internal/module/ai` RED 测试
- 实现 intake / feedback 模型、memory repository、service 逻辑
- 再补 HTTP RED 测试并实现 `feedback / stats` handler 与路由
- 最后补 app 集成测试，完成装配与 schema 升级

### 5. 持久化补齐

- 新增 `ai_unified_intakes`
- 新增 `ai_answer_feedbacks`
- 补齐 postgres repository 与 sqlmock 测试

### 6. 收口验证

- 执行 `gofmt`
- 执行 `GOCACHE=/tmp/go-build go test -count=1 ./...`
- 更新 README 与工作包文档
