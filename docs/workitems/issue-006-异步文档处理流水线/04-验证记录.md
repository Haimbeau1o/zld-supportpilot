# 验证记录

## 自动化验证

| 命令 | 结果 | 说明 |
| --- | --- | --- |
| `go test -count=1 ./internal/module/knowledge` | 通过 | 覆盖文档上传、任务创建、同步处理、失败重试、对象存储回读等核心领域行为 |
| `go test -count=1 ./internal/platform/http` | 通过 | 覆盖知识库、文档上传、任务查询、失败文档重试等 HTTP 契约 |
| `go test -count=1 ./internal/app` | 通过 | 验证应用装配、鉴权主链路和异步 worker 已接入 |
| `go test -count=1 ./...` | 通过 | 全量回归通过，未引入跨模块回退 |

## 手工验证

- 通过 `TestNewWiresKnowledgeAsyncProcessing` 模拟真实请求链路，验证上传接口先返回 `uploaded`，随后后台任务把文档推进到 `ready`。
- 通过 `TestKnowledgeRetryDocument` 模拟入队失败，验证系统会生成失败任务，并可通过重试接口创建第 2 次处理任务。

## 结论

- `#6` 的核心验收标准已满足：上传 API 与耗时处理解耦、失败任务可追踪、人工重试可用、切块与索引边界已稳定。
