# 工作日志

## 2026-03-07

- 确认 `#17` 已于 `2026-03-07` 合并到 `origin/main`，`#6` 可以直接基于主干展开。
- 在 `.worktrees/codex-async-document-pipeline` 创建隔离工作树，避免与其他 issue 相互污染。
- 完成 `#6` 的任务卡、调研记录、方案设计和实施计划初稿。
- 明确设计基线：采用“文档 + 处理任务 + 后台 worker”三层模型，并保留 parser/chunker/indexer 边界。
- 通过 TDD 先补失败测试，再落地处理任务模型、对象存储回读、切块仓储和异步队列。
- 在 `knowledge.Service` 中补齐上传后入队、同步 `ProcessTask`、失败沉淀和人工重试逻辑。
- 在 HTTP 层新增任务查询与重试接口，并在应用层装配内存 worker 与关闭逻辑。
- 使用独立 `GOCACHE` 完成分层回归和全量回归，避免沙箱缓存目录带来的权限问题。
