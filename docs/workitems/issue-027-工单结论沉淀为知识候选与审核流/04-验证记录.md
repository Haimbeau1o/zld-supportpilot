# 验证记录

## 基线验证

- 时间：2026-03-09
- 分支：`codex/ticket-knowledge-candidates`
- 基线来源：`origin/main`
- main HEAD：`2517d05`
- 命令：`GOCACHE=/tmp/go-build go test -count=1 ./...`
- 结果：通过

## TDD 过程验证

### 1. knowledge 域 RED

- 命令：`GOCACHE=/tmp/go-build go test -count=1 ./internal/module/knowledge`
- 初始结果：失败
- 失败原因：缺少 `KnowledgeCandidate` 模型、仓储与 service 能力
- 结论：RED 成立，可进入最小实现

### 2. knowledge 域 GREEN

- 命令：`GOCACHE=/tmp/go-build go test -count=1 ./internal/module/knowledge`
- 结果：通过
- 覆盖点：
  - 已解决工单可创建知识候选
  - 非 resolved 工单会被拒绝
  - 审核通过后自动生成 markdown 文档并进入处理队列
  - 审核拒绝不会生成知识文档
  - memory / postgres 两套候选仓储均可工作

### 3. HTTP API RED -> GREEN

- RED 命令：`GOCACHE=/tmp/go-build go test -count=1 ./internal/platform/http`
- RED 现象：`/api/v1/knowledge/candidates` 系列路由返回 `404`
- GREEN 命令：`GOCACHE=/tmp/go-build go test -count=1 ./internal/platform/http`
- GREEN 结果：通过
- 覆盖点：
  - 候选创建接口
  - 候选列表接口
  - 候选审核接口
  - 错误映射与权限边界

### 4. app 端到端 RED -> GREEN

- RED 命令：`GOCACHE=/tmp/go-build go test -count=1 ./internal/app -run TestNewKnowledgeCandidateApprovalFeedsAIAnswer`
- RED 现象：应用装配未启用候选审核流，返回 `503 knowledge_candidate_workflow_unavailable`
- GREEN 命令：`GOCACHE=/tmp/go-build go test -count=1 ./internal/app -run TestNewKnowledgeCandidateApprovalFeedsAIAnswer`
- GREEN 结果：通过
- 覆盖点：
  - 工单创建并流转到 resolved
  - 候选创建并审核通过
  - 审批后生成知识文档并异步处理完成
  - AI 问答能够命中候选沉淀内容

## 回归验证

- 命令：`GOCACHE=/tmp/go-build go test -count=1 ./...`
- 结果：通过
- 说明：当前分支所有 Go 包回归通过，无额外已知失败项
