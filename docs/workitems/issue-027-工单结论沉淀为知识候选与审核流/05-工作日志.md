# 工作日志

## 2026-03-09

### 1. 依赖判断

- 确认 `#24` 已合入主干
- 确认 `#25`、`#26` 已通过 `#34` 合入 `main`
- 判断 `#27` 应直接从最新 `main` 开发，而不是继续 stacked 在旧分支上

### 2. 隔离环境

- 创建工作树：`codex/ticket-knowledge-candidates`
- 路径：`/Volumes/passport/简历/go_learn/zld-supportpilot/.worktrees/codex-ticket-knowledge-candidates`
- 执行主干基线回归，确认可继续开发

### 3. 代码侦察

- 调研 ticket resolved 状态与时间线能力
- 调研 knowledge 上传、对象存储、异步处理、切块、索引链路
- 确认“审核通过后生成 markdown 文档并复用现有知识处理链路”是最小合理方案

### 4. TDD 执行

- 先编写 `candidate_test.go`，明确 service 契约
- 运行 RED，确认缺失点集中在候选模型、仓储与审核流能力
- 实现 `KnowledgeCandidate` 模型、memory repository、service 规则与 markdown 生成逻辑
- 将审核通过动作接到现有 `UploadDocument -> scheduleDocumentProcessing -> ProcessTask` 链路

### 5. API 与应用装配

- 先补 HTTP 契约测试，确认候选创建 / 列表 / 审核三个接口行为
- 新增候选路由、请求响应结构与错误映射
- 在 `app.New()` 中接入候选仓储和 `ticketService` 作为 `TicketSource`
- 追加端到端用例，验证“resolved ticket -> candidate -> review approve -> document ready -> AI answer”

### 6. 持久化与收口

- 补充 PostgreSQL `knowledge_candidates` 表结构
- 实现 postgres repository 与 sqlmock 测试
- 更新 README、验证记录、工作日志与复盘总结
