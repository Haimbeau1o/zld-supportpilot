# 工作日志

## 2026-03-07

- 确认 `#6` 已于 `2026-03-07` 合并到 `origin/main`，`#7` 可以直接从主干启动。
- 在 `.worktrees/codex-rag-answer-api` 创建隔离工作树，确保与其他 issue 隔离。
- 完成 `#7` 的任务卡、调研记录、方案设计和实施计划初稿。
- 选定实现基线：`ai` 模块独立负责检索与回答，`knowledge` 模块提供 ready chunk 读取边界。
- 通过 TDD 先实现向量检索、再实现回答与降级逻辑，最后补齐 HTTP 和 app 链路。
- 使用 deterministic hashing embedding + cosine similarity 模拟向量检索，先稳定接口和评分逻辑。
- 在 `knowledge.Service` 中补充 indexed chunk 读取能力，使 AI 模块可以在不碰对象存储的前提下消费知识结果。
