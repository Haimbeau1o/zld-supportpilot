# 验证记录

## 自动化验证

| 命令 | 结果 | 说明 |
| --- | --- | --- |
| `go test -count=1 ./internal/module/ai` | 通过 | 覆盖向量检索排序、回答生成和低置信度降级逻辑 |
| `go test -count=1 ./internal/platform/http` | 通过 | 覆盖 AI 回答接口、知识接口和认证边界 |
| `go test -count=1 ./internal/app` | 通过 | 覆盖上传 -> 异步处理 -> RAG 回答的整链路装配 |
| `go test -count=1 ./...` | 通过 | 全量回归通过，未引入跨模块回退 |

## 手工验证

- 通过 `TestNewWiresRAGAnswerRoute` 模拟真实链路，验证知识文档处理完成后，可通过 AI 回答接口命中相关 chunk 并返回引用。
- 通过 `TestAIAnswerQuestionDegradesWhenLowConfidence` 验证低相关问题会返回 `degraded`，而不是伪造答案。

## 结论

- `#7` 的核心验收标准已满足：回答接口可返回引用或明确降级结果，检索与生成职责边界清晰。
