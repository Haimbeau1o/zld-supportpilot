# 验证记录

## 基线验证

- 时间：2026-03-09
- 分支：`codex/issue-029-notification-touchpoint`
- 命令：`GOCACHE=/tmp/go-build go test -count=1 ./...`
- 结果：通过

## RED / GREEN 验证

### 1. 通知模块

新增测试：

- `TestServicePublishContinuesWhenChannelFails`
- `TestEmailChannelSendsMailToResolvedRecipients`
- `TestWebhookChannelPostsStructuredEvent`

验证点：

- 多渠道扇出时，单个渠道失败不会阻断其他渠道
- 邮件渠道会跳过无邮箱用户，只向可解析邮箱发送
- Webhook 渠道会发送结构化 JSON 事件

结果：通过

### 2. 工单侧通知挂点

新增测试：

- `TestAssignTicketPublishesNotification`
- `TestAssignTicketIgnoresNotificationFailure`
- `TestTransitionTicketStatusPublishesNotification`

验证点：

- 分派时会发布通知事件
- 状态流转时会发布通知事件
- 通知失败不会阻断工单主流程成功返回

结果：通过

### 3. AI 升级通知挂点

新增测试：

- `TestHandleUnifiedIntakePublishesEscalationNotificationWhenKnowledgeIsDegraded`
- `TestSubmitAnswerFeedbackPublishesEscalationNotificationWhenTicketCreated`
- `TestSubmitAnswerFeedbackIgnoresNotificationFailure`

验证点：

- AI 自助降级建单时会发布升级通知
- unresolved 反馈升级建单时会发布升级通知
- 通知失败不会阻断升级建单结果

结果：通过

## 配置与装配验证

- `internal/app` 已完成通知模块装配
- memory / postgres 两种持久化模式都会装配同一套通知抽象
- 默认启用邮件日志 sender，Webhook 由环境变量按需开启

## 全量回归

- 命令：`GOCACHE=/tmp/go-build go test -count=1 ./...`
- 结果：通过

## 结论

`#29` 已完成最小可用通知触达能力：

- 有统一通知抽象
- 有邮件 / Webhook 两个可替换渠道
- 分派 / 状态变化 / AI 升级已能触发通知
- 通知失败不会阻塞主业务流程
