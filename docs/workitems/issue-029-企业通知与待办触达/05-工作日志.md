# 工作日志

## 2026-03-09

- 从 `origin/main` 创建独立 worktree：`codex/issue-029-notification-touchpoint`
- 完成基线回归，确认主线当前可作为 `#29` 的实现底座
- 调研 `ticket`、`ai`、`identity` 三个模块，确认通知挂点与收件人解析缺口
- 收敛方案为：独立通知模块 + 邮件 / Webhook 渠道 + 业务侧最佳努力发布
- 建立 `docs/plans/` 与 `docs/workitems/issue-029-企业通知与待办触达/` 工作包
- 在 issue #29 留言同步分支、计划与调研结论

### RED 阶段

- 新增 `internal/module/notify/service_test.go`
- 新增 `internal/module/ticket/notification_test.go`
- 新增 `internal/module/ai/notification_test.go`
- 首次执行时，预期出现编译失败，证明通知抽象与业务挂点尚未实现

### GREEN 阶段

- 新增 `internal/module/notify/` 模块，落地：
  - 事件模型
  - 发布服务
  - 邮件渠道
  - Webhook 渠道
- 补齐 `identity` 用户查询能力，用于按 userID 解析邮箱
- 在 `ticket.Service` 中挂接：
  - 分派通知
  - 状态变更通知
- 在 `ai.Service` 中挂接：
  - 降级建单通知
  - unresolved 升级通知
- 在 `internal/app` 中完成通知模块装配与配置接线
- 更新 `README.md`，补充通知能力和环境变量说明

### 调试记录

- 通知模块 Webhook 测试最初使用 `httptest.NewServer`，但当前沙箱环境不允许监听端口
- 改为 stub HTTP client 后，测试仍能覆盖结构化请求体校验，同时避免环境依赖
- `internal/app` 初次 wiring 时，`notificationService` 定义在 switch 分支局部作用域，导致 `ai` 装配编译失败
- 调整为外层共享变量后，`ticket` 与 `ai` 成功复用同一通知服务实例

### 收口验证

- 先通过聚焦测试：`./internal/module/notify ./internal/module/ticket ./internal/module/ai ./internal/module/identity`
- 再执行 `gofmt`
- 最后执行全量回归：`GOCACHE=/tmp/go-build go test -count=1 ./...`
