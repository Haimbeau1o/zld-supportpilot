# 验证记录

## 自动化验证

| 命令 | 结果 | 说明 |
| --- | --- | --- |
| `go test -count=1 ./...` | 已通过（基线） | `#9` 开始前确认主干基线稳定 |
| `go test -count=1 ./internal/platform/config` | 已通过 | 覆盖限流默认值、环境变量覆盖与非法值回退 |
| `go test -count=1 ./internal/platform/http` | 已通过 | 覆盖请求 ID、结构化日志、指标快照与限流行为 |
| `go test -count=1 ./internal/app` | 已通过 | 验证应用装配未回归 |
| `go test -count=1 ./...` | 已通过 | 全量回归成功 |

## 手工验证

- 本次会话未单独启动服务做交互式手工演示；
- 已在 `README.md` 中补充可直接复现的观测、指标和限流演示步骤；
- 当前阶段主要通过自动化测试证明行为正确，并为后续演示保留标准命令脚本。

## 边界情况验证

- 请求头自带 `X-Request-ID` 时，响应会复用该值；
- 未超限请求保持原有成功行为，不影响已有业务测试；
- 触发 `429` 后仍保持统一 JSON 错误结构，并返回 `Retry-After`；
- 关键路由指标与限流命中次数可通过 `/debug/metrics/http` 查看。

## 失败与修正

- 首轮红灯失败点主要集中在新字段和中间件实现缺失，符合 TDD 预期；
- 实现阶段没有出现额外的业务回归，只需补齐统一装配与文档说明即可通过回归测试。
